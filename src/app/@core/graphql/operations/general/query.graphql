query GetTimeZoneList {
    timeZones {
        id
        displayName
        observesDaylightTime
        offset
    }
}

query GetCurrencyList {
    currencies {
        code
        displayName
        symbol
    }
}
